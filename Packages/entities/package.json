{
  "name": "entities",
  "version": "0.1.0",
  "description": "Entities module",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "rm -f entities-0.1.0.tgz && tsc",
    "build-pack": "npm run build && npm pack",
    "install-packages": "npm install --no-package-lock --prefer-offline",
    "bpc": "npm run install-packages && npm run build-pack && cp entities-0.1.0.tgz ../dist/entities-0.1.0.tgz",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle Finley",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.2",
    "socket.io-client": "^2.3.0",
    "vue": "^2.6.11",
    "vuex": "^3.1.3",
    "vuex-module-decorators": "^0.17.0"
  },
  "devDependencies": {
    "@types/node": "^13.7.0",
    "@typescript-eslint/eslint-plugin": "^2.32.0",
    "@typescript-eslint/parser": "^2.32.0",
    "@types/socket.io-client": "^1.4.32",
    "eslint": "^6.8.0",
    "json-schema": "^0.2.5",
    "typescript": "^3.8.3"
  },
  "files": [
    "dist/*",
    "src/*",
    "README.md",
    "package.json"
  ]
}
